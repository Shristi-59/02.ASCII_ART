# 02.ASCII_ART
The Image to ASCII Art Converter is a Python-based image processing project that converts digital images into text-based ASCII art. The project uses OpenCV for image reading, grayscale conversion, and resizing, while NumPy is used to process pixel data.
